import { jsonformsReactToolkit } from './jsonforms-react-toolkit';

describe('jsonformsReactToolkit', () => {
  it('should work', () => {
    expect(jsonformsReactToolkit()).toEqual('jsonforms-react-toolkit');
  });
});
